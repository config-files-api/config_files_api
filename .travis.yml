language: ruby
rvm:
  - 2.1.9
addons:
  apt:
    packages:
      - libaugeas-dev
      - pkg-config
# use trusty travis infrastructure
sudo: required
dist: trusty
install: gem install rspec ruby-augeas coveralls
script:
    - COVERAGE=1 rake spec

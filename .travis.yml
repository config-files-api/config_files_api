language: ruby
rvm:
  - 2.2.3
  - 2.1.7
addons:
  apt:
    packages:
      libaugeas-dev
      pkg-config
# use new travis infrastructure
sudo: false
install: gem install rubocop rspec ruby-augeas
script:
    - rake spec
    - rubocop
